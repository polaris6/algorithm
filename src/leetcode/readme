1     两数之和                给一个整数数组和一个目标值，找出数组中和为目标值的两个数，返回两数的下标
3     无重复字符的最长子串    给一个字符串，返回它的不包含重复字符的最长子串的长度
5     最长回文子串            给一个字符串s，找到s中最长的回文子串
53    最大子序和              给一个整数数组，找到一个具有最大和的连续子数组，返回其最大和
62    不同路径                机器人位于一个m x n网格的左上角，每次只能向右或向下移动一步，共有多少条路径
63    不同路径二              接上一题，考虑路径中有障碍物，障碍物和空位置分别用1和0来表示
64    最小路径和              给定一个包含非负整数的m x n网格，找出一条从左上角到右下角的路径，使路径上的数字总和最小
70    爬楼梯                  楼梯有n个台阶，每次可以爬1或2个台阶，有多少种方法可以到达楼顶
72    编辑距离                给定两个单词word1和word2，计算出将word1转换成word2所使用的最少操作数（未做）
85    最大矩形                给一个仅包含0和1的二维二进制矩阵，找出只包含1的最大矩形，并返回其面积（未做）
442   数组中重复的数据
560   和为k的子数组           给一个整数数组和一个整数k，找到数组中和为k的连续的子数组的个数
665   非递减数列              给一个整数数组，判断在最多改变1个元素的情况下，该数组能否变成一个非递减数列
674   最长连续递增序列        给一个未经排序的整数数组，找到最长连续的的递增序列，返回序列长度

413   等差数组划分            返回数组A中所有为等差数组的子数组个数
416   分割等和子集            判断是否可以将一个只包含正整数的非空数组分割成两个子集，使得两个子集的元素和相等
464   先出手的玩家能赢吗      给定一个整数（整数池中可选择的最大数）和累计和，判断先出手的玩家能否先达到累计和
466   统计重复次数            S=[s,n]（字符串，重复次数），S1=[s1,n1]、S2=[s2,n2]，找出可以使[S2,M]从S1获得的最大整数M
472   连接词                  给定一个不含重复单词的列表，返回给定单词列表中所有的连接词（能由数组中至少两个单词连接而成）
474   0和1拼凑字符串          使用给定的m个0和n个1，找到能拼出存在于数组中的字符串的最大数量，每个0和1至多被使用一次
486   预测赢家                给定一个表示分数的非负整数数组，玩家1从数组任意一端拿一个分数，随后玩家2继续从剩余数组任意
                              一端拿取分数....直至拿完，分数总和最多的玩家获胜，判断玩家1能否获胜
494   目标和                  给定一个非负整数数组和一个目标数S，选择+或-符号添加在整数前面，返回使总和为S的所有方法数
516   最长回文子序列          给定一个字符串s，找到其中最长的回文子序列
517   超级洗衣机              n台洗衣机放在同一排上，每台洗衣机内有一定量的衣服（也可能为空），每次选择任意m台洗衣机，
                              将每台洗衣机的一件衣服送到相邻的另一台，求让所有洗衣机中剩下衣物的数量相等的最少操作次数

