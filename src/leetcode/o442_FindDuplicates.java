package leetcode;

import java.util.ArrayList;
import java.util.List;

/**
 * 题目描述：数组中重复的数据
 * 给定一个长度为n的整数数组a，其中1 < a[i] < n，有些元素出现两次而其他元素出现一次，找出所有出现两次的元素
 * 要求时间复杂度o(n)，并且不能使用额外空间
 */
public class o442_FindDuplicates {
//    public List<Integer> findDuplicates(int[] nums) {
//        List<Integer> list = new ArrayList<>();
//        if(nums.length == 0)
//            return list;
//
//    }
}